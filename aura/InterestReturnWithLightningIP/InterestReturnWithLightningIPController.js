({
    calc : function(component, event, helper) {
        helper.calcHelper(component, event, helper);
    },
    validateAmount  : function(component, event, helper){
        helper.validateAmount(component, event, helper);
    },
    validateDuration  : function(component, event, helper){
        helper.validateDuration(component, event, helper);
    },
    validateInterest  : function(component, event, helper){
        helper.validateInterest(component, event, helper);
    }
    
    
})